# 智能提醒项目交接文档

本文档用于把当前项目交给其他 AI 或开发者继续优化。交接对象可以只读本文档，再结合 `README.md`、`docs/superpowers/specs/` 和 `docs/superpowers/plans/` 继续工作。

更新日期：2026-08-08

## 当前代码状态

- 仓库：`https://github.com/lewyoung1989-lang/smart-reminder`
- 本地主要工作区：`/Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration`
- 当前测试分支：`release/full-integration-20260807`
- 当前主分支：`main`
- 当前最新提交：`82b3f31 Prefill local test login credentials`
- `main` 与 `release/full-integration-20260807` 已同步到同一个提交。
- 用户要求：新增或修改的 Markdown 文档必须使用中文。

截至本交接时，GitHub 远端 `main` 和 `release/full-integration-20260807` 都指向：

```text
82b3f31682c53878787e897273402904e75db510
```

本地仍有一个未跟踪文件：

```text
backend/tests/test_settings.py
```

不要默认提交、删除或修改这个文件，除非用户明确确认它属于当前任务。

## 产品定位

这是一个“智能提醒 App”，不是泛化聊天助手。

V1 的核心思路是：

- 模型负责理解用户自然语言，生成结构化草稿。
- 系统只执行经过验证的工作流组件。
- 用户确认前不创建正式提醒。
- 新类型首次确认；用户信任后，同类请求未来可减少确认。

当前已经优先做的是 A+C 路线：

- A：明确语义提醒，例如“明天晚上 8 点提醒我洗澡”。
- C：固定且可验证的工作流组件，例如吃药、药品有效期、出门提醒、今日行动中心。

未来计划是 B+C：

- B：更主动的智能助手能力，例如根据日程、天气、交通自动生成提醒建议。
- C：仍然通过受控工作流执行，避免模型直接操作系统或外部服务。

V2 方向写在：

```text
docs/superpowers/specs/2026-08-05-smart-reminder-v2-enhancements.md
```

## 当前已完成的主要能力

### iOS / Flutter App

- iPhone 可安装 profile 包并从桌面直接打开。
- App 采用 Flutter 实现，整体 UI 已按 iOS HIG 做过一轮重设计。
- 底部主入口包括：
  - 提醒
  - 药箱
  - 拍照录入
  - 我的
- 登录页当前默认填充本地测试账号：

```text
手机号：13800138000
密码：Test-pass-2026
```

这个默认填充是为了本地真机调试方便，后续做正式发布前应移除，或改成只在 Debug/Profile 内测环境启用。

### 账号认证

- 支持手机号注册、登录、刷新 Token、退出登录、修改密码。
- App 使用 JWT，并把 Token 保存在 iOS Keychain。
- 当前本地测试库已经初始化账号：

```text
手机号：13800138000
密码：Test-pass-2026
```

### 文字提醒

- 支持输入中文自然语言创建提醒草稿。
- 本地规则可解析：
  - “今天/明天几点”
  - “几分钟后”
- 本地规则无法确定时，会尝试 DeepSeek 兜底。
- 模型输出会经过 Pydantic 严格校验。
- 所有草稿必须人工确认后才创建正式提醒。
- 确认后 App 会安排 iPhone 本地通知。

已修复一个关键体验问题：

- 后端提醒创建成功，但 iPhone 本地通知调度失败时，页面会退出确认页并提示“提醒已创建，但手机通知未安排”，避免用户误以为创建失败。

### 模型解析

本地 `.env` 已配置 DeepSeek API Key，但密钥不在 Git 中，不能写入文档、日志或提交。

曾出现的问题：

- 页面显示“本地规则（模型不可用）”。
- 根因不是 Key 缺失，而是本机 Python 请求 DeepSeek 时 SSL 校验失败：

```text
self-signed certificate in certificate chain
```

当前本地已通过在 `.env` 中设置 `SSL_CERT_FILE` 指向虚拟环境的 `certifi` 证书文件解决：

```text
SSL_CERT_FILE=/Users/liuyang/Desktop/own/smart-reminder/.venv/lib/python3.13/site-packages/certifi/cacert.pem
```

无个人信息测试句已验证可走 DeepSeek：

```text
下周一上午十点提醒我测试
```

返回结果：

```text
parser_source=deepseek
has_schedule=True
title=测试
```

### 语音输入

- App 有语音入口。
- 设计目标是 iPhone 录音后上传到私有 FunASR 服务转写为文字。
- 用户当前决定：语音识别问题先等备案/域名恢复后再调整。
- 不要急着把音频发到第三方，也不要为了演示绕过隐私边界。

### 药箱与 OCR

- 已接入药品库存 API。
- 支持药箱列表、批次详情、有效期状态。
- 支持拍照录入药盒，OCR 候选结果需要人工确认后写入药箱。
- 支持修改药品有效期。

### 行动中心 / 工作流

- 已有“今日行动中心”接口和 App 展示。
- 已有 typed workflow 的基础结构。
- 已实现或接入的首批闭环包括：
  - 吃药周期提醒
  - 药品有效期管理
  - 出门/行程类工作流基础能力

## 本地真机调试状态

当前推荐继续用 Mac 局域网 IP，不走域名：

```text
API_BASE_URL=http://192.168.71.132:8000
```

本地后端启动方式：

```bash
/Users/liuyang/Desktop/own/smart-reminder/.venv/bin/python backend/manage.py runserver 0.0.0.0:8000
```

当前本地 `.env` 的关键点：

- `DJANGO_ALLOWED_HOSTS` 包含 `192.168.71.132`
- `S3_PUBLIC_ENDPOINT` 指向 `http://192.168.71.132:9000`
- `DEEPSEEK_API_KEY` 已设置，但不能提交
- `SSL_CERT_FILE` 已设置，用于解决本机 DeepSeek SSL 证书链问题
- 本地直跑 Django 时使用 SQLite，不依赖 Docker PostgreSQL

健康检查：

```bash
curl -i http://127.0.0.1:8000/api/v1/health
curl -i http://192.168.71.132:8000/api/v1/health
```

预期返回：

```text
HTTP/1.1 200 OK
{"status": "ok", "service": "smart-reminder-api"}
```

## iPhone 构建与安装

Flutter SDK 位于：

```text
/Users/liuyang/Desktop/own/.toolchains/flutter-sdk/flutter
```

Pub cache 位于：

```text
/Users/liuyang/Desktop/own/.toolchains/pub-cache
```

iPhone 设备信息：

```text
设备名：刘洋的 iPhone
devicectl UUID：E017E520-1D38-5853-8BA0-B1F0A312B72E
Flutter UDID：00008150-000E09290C85401C
Bundle ID：com.liuyang.smartreminder.smartReminderApp
App 名称：智能提醒
```

构建 profile 包：

```bash
cd /Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration/app

FLUTTER_SUPPRESS_ANALYTICS=true \
FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn \
PUB_CACHE=/Users/liuyang/Desktop/own/.toolchains/pub-cache \
PATH=/opt/homebrew/bin:/opt/homebrew/sbin:$PATH \
/Users/liuyang/Desktop/own/.toolchains/flutter-sdk/flutter/bin/flutter build ios \
  --profile \
  --dart-define=API_BASE_URL=http://192.168.71.132:8000
```

安装到 iPhone：

```bash
xcrun devicectl device install app \
  --device E017E520-1D38-5853-8BA0-B1F0A312B72E \
  build/ios/iphoneos/Runner.app
```

注意：

- Debug 包不能从 iPhone 桌面直接打开，曾导致闪退。
- 原因是 Flutter debug 包不能脱离 Flutter tooling 或 Xcode 创建 `FlutterEngine`。
- 真机桌面打开请使用 profile 包或 release 包。

## 服务器部署状态

腾讯云服务器已有部署，部署目录：

```text
/opt/smart-reminder/app
```

服务器曾部署到：

```text
5a98489d0f82b7600fe772140dfd69a5cf6cce73
```

容器状态曾验证：

- `api` healthy
- `funasr` healthy
- `postgres` healthy
- `redis` healthy
- `nginx` 监听 80/443
- `worker` running

服务器内部 HTTPS health 和公网 IP + Host 头曾验证成功。

当前线上阻塞点：

- `aipupu.cloud`
- `files.aipupu.cloud`

域名公网访问会被 DNSPod/备案相关管控拦截或 reset。

结论：

- 服务器代码和容器本身不是主要问题。
- 线上完整验证需要等备案/域名恢复。
- 短期不要把 App 默认切到域名。
- 如果必须绕过域名，可做临时 IP 调试入口，但需要额外处理 Nginx、Django `ALLOWED_HOSTS`、HTTPS 证书与文件地址。

## 验证记录

合并到 `main` 前最近一次验证：

### Flutter

```bash
cd app
FLUTTER_SUPPRESS_ANALYTICS=true \
PUB_CACHE=/Users/liuyang/Desktop/own/.toolchains/pub-cache \
/Users/liuyang/Desktop/own/.toolchains/flutter-sdk/flutter/bin/flutter test
```

结果：

```text
223 passed
```

登录页单测也单独跑通过：

```bash
flutter test test/auth_screen_test.dart
```

结果：

```text
All tests passed
```

静态检查：

```bash
flutter analyze
```

结果：

```text
No issues found
```

### 后端

由于本地 `.env` 为真机调试写入了 Redis/FunASR 等地址，直接跑后端全量测试会被 `.env` 污染。测试时应显式覆盖：

```bash
AUTH_CACHE_URL= \
ASR_THROTTLE_REDIS_URL= \
ASR_BASE_URL=http://localhost:18001 \
/Users/liuyang/Desktop/own/smart-reminder/.venv/bin/pytest backend/tests -q
```

结果：

```text
passed，2 skipped
```

有 JWT HMAC key 长度 warning，当前不是失败项。生产环境应使用足够长度的 `JWT_SIGNING_KEY`。

## 当前已知问题与建议优先级

### P0：不要泄露本地密钥

- `.env` 已被 Git 忽略。
- DeepSeek API Key 不能写入文档、日志、测试、截图或提交。
- 接手 AI 如果需要验证模型，应优先使用无个人信息测试句。

### P0：生产发布前移除登录页默认密码

当前登录页默认填充：

```text
13800138000 / Test-pass-2026
```

这是为了本地真机调试方便。正式发布前应改成：

- 仅 Debug/Profile 内测构建启用；或
- 通过 `--dart-define` 控制；或
- 完全移除。

推荐方式：

```text
--dart-define=PREFILL_TEST_LOGIN=true
```

不要让正式 release 包默认带测试账号密码。

### P1：增强本地时间解析

当前本地规则还不够自然。比如：

```text
晚上9点我要洗澡
我晚上8点要出门去地铁站
```

如果模型不可用，会显示“时间待补充”。建议增强规则：

- 支持“晚上/今晚/下午/早上/中午/凌晨”
- 没有日期但有时间时：
  - 如果时间未过，默认今天
  - 如果时间已过，默认明天
- 标题提取从“我要/要/提醒我/去”等结构中提取，例如“洗澡”“出门去地铁站”

这个优化不依赖备案，可以先做。

### P1：把“模型不可用”原因拆细

当前 App 显示：

```text
本地规则（模型不可用）
```

但真实原因可能是：

- 未配置 API Key
- SSL 证书错误
- 模型网络超时
- 模型返回格式不合规
- 模型拒绝或服务端错误

建议后端返回更细的错误码，App 展示更可操作的说明。

### P1：测试环境不要被本地 `.env` 污染

目前 `settings.py` 会自动加载仓库根目录 `.env`。这对开发方便，但会污染 pytest。

建议增加测试专用开关，例如：

```text
DJANGO_SKIP_DOTENV=true
```

或者在 `pytest.ini` / `conftest.py` 中统一覆盖缓存与 ASR 相关环境变量。

### P1：服务器域名恢复后做线上回归

备案/域名恢复后，需要重新验证：

- `https://aipupu.cloud/api/v1/health`
- `https://files.aipupu.cloud/`
- 登录、刷新、退出
- 文字提醒模型兜底
- OCR 上传、识别、确认
- FunASR 语音识别
- Nginx、证书续期、备份、回滚

### P2：语音识别体验

用户当前同意等备案完成后再处理语音识别。后续优化建议：

- 增加能力检测文案，区分“服务未启动”“服务忙”“网络错误”“权限拒绝”
- 真机录音链路做端到端测试
- 保持“识别只填入文字，不自动创建提醒”的安全边界

### P2：工作流智能化

未来 B+C 路线建议：

- 让模型负责把自然语言映射到工作流模板
- 所有执行动作仍由固定组件完成
- 新类型首次确认，可信后可自动创建同类提醒
- 天气、交通、节假日、票务、信用卡还款等作为可验证 Provider 接入

## 推荐下一步任务

如果交给其他 AI，推荐按这个顺序继续：

1. 修复本地时间解析：支持“晚上9点我要洗澡”这类表达。
2. 增加模型不可用错误码和 App 展示。
3. 把登录页测试账号预填改成 `--dart-define` 控制。
4. 增加测试环境跳过 `.env` 的机制，避免 pytest 被本地调试配置污染。
5. 等备案恢复后做线上部署回归。
6. 再处理 FunASR 语音识别真机链路。
7. 再推进 V2 智能工作流。

## 常用命令

### 查看当前分支与远端

```bash
git status --short --branch
git log --oneline --decorate -8
git ls-remote origin refs/heads/main refs/heads/release/full-integration-20260807
```

### Flutter 测试

```bash
cd /Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration/app

FLUTTER_SUPPRESS_ANALYTICS=true \
PUB_CACHE=/Users/liuyang/Desktop/own/.toolchains/pub-cache \
/Users/liuyang/Desktop/own/.toolchains/flutter-sdk/flutter/bin/flutter test
```

### Flutter 静态检查

```bash
cd /Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration/app

FLUTTER_SUPPRESS_ANALYTICS=true \
PUB_CACHE=/Users/liuyang/Desktop/own/.toolchains/pub-cache \
/Users/liuyang/Desktop/own/.toolchains/flutter-sdk/flutter/bin/flutter analyze
```

### 后端测试

```bash
cd /Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration

AUTH_CACHE_URL= \
ASR_THROTTLE_REDIS_URL= \
ASR_BASE_URL=http://localhost:18001 \
/Users/liuyang/Desktop/own/smart-reminder/.venv/bin/pytest backend/tests -q
```

### 本地启动后端

```bash
cd /Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration

/Users/liuyang/Desktop/own/smart-reminder/.venv/bin/python backend/manage.py migrate
/Users/liuyang/Desktop/own/smart-reminder/.venv/bin/python backend/manage.py runserver 0.0.0.0:8000
```

### 构建并安装 iPhone profile 包

```bash
cd /Users/liuyang/Desktop/own/smart-reminder/.worktrees/flutter-ui-ocr-integration/app

FLUTTER_SUPPRESS_ANALYTICS=true \
FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn \
PUB_CACHE=/Users/liuyang/Desktop/own/.toolchains/pub-cache \
PATH=/opt/homebrew/bin:/opt/homebrew/sbin:$PATH \
/Users/liuyang/Desktop/own/.toolchains/flutter-sdk/flutter/bin/flutter build ios \
  --profile \
  --dart-define=API_BASE_URL=http://192.168.71.132:8000

xcrun devicectl device install app \
  --device E017E520-1D38-5853-8BA0-B1F0A312B72E \
  build/ios/iphoneos/Runner.app
```

## 交接提醒

- 不要把这个项目继续做成无限权限的“全能助手”。当前产品更稳的方向是“提醒和生活工作流助手”。
- 模型只负责理解和组装，执行必须通过已验证组件。
- 用户非常重视真机验证、服务器验证和 GitHub 状态实证，不要只说“应该可以”。
- 所有 Markdown 文件写中文。
- 对外部服务、密钥、个人日程文本要保守处理。
